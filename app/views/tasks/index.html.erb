<!DOCTYPE html>
<html lang="ja">
<p>ログイン完了ユーザー: <%= current_user.name %>....<%= @tasks.count %>個のタスク</p>
<h1><%= current_user.name %>さんのタスク一覧</h1>
<table border="1"><!--#cellpaddingはHTML5で非推奨-->
    <thead>
        <tr>
            <th>タスク</th>
            <th>ステータス</th>
            <th>登録日時</th>
            <th>詳細</th>
            <th>削除</th>
        </tr>
    </thead>
    <tbody>
        <% @tasks.each do |task| %>
        <tr>
            <th><%= task.content %></th>
            <th><%= task.status %></th>
            <th><%= task.created_at %></th>
            <th><%= link_to '詳細/編集',task %></th>
            <% if current_user == task.user %>
                <th><%= link_to "Delete", task, method: :delete, data: {confirm:"You sure?" }, class: 'btn' %></th>
            <% end %>
        </tr>
        <% end %>
    </tbody>
</table>    
<ul>
    <li><%= link_to '新規タスクの投稿', new_task_path %></li>
    <li><%= link_to 'Logout', logout_path, method: :delete %></li>
</ul>