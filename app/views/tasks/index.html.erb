<p>ログイン完了ユーザー: <%= current_user.name %>....<%= @tasks.count %>個のタスク</p>
<h1><%= current_user.name %>さんのタスク一覧</h1>
<table border="1" cellpadding="5">
    <thead>
        <tr>
            <th>タスク</th>
            <th>ステータス</th>
            <th>登録日時</th>
            <th>削除</th>
      </tr>
    </thead>
    <tbody>
      
        <% @tasks.each do |task| %><%#= link_to task.user.name,user_path(task.user) %> 
        <tr>
            <th><%= task.content %></th>
            <th><%= task.status %></th>
            <th><%= task.created_at %></th>
            <% if current_user == task.user %>
            <th><%= link_to "Delete", task, method: :delete, data: {confirm:"You sure?" }, class: 'btn' %></th>
        </tr>
        <% end %>
<% end %>
</tbody>
</table>    
<ul>
    <li><%= link_to '新規タスクの投稿', new_task_path %></li>
    <%#= link_to 'My profile', user_path(current_user) %>
    <li><%= link_to 'Logout', logout_path, method: :delete %></li>
</ul>    
  
  
  
  
  
  
  
  <%#= render 'tasks/tasks', tasks: @tasks %>
